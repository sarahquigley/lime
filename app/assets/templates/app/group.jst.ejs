<div class="index-show">
  <% if(group.title){ %>
  <header class="inbox-header">
    <h3><%= group.title %></h3>
  </header>
  <% } %>
</div>

<!-- <div class="inbox-hidden <% if(group.display){ %>hidden<% } %>">
  <button class="toggle-inbox">Show <%= group.title %> tasks.</button>
</div> -->

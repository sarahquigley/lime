<ul>
  <% tasks.each(function(task){ %>
  <li id="task-<%= task.get('id') %>" class="task" data-task-id="<%= task.get('id') %>">
    <input
      type="checkbox"
      name="task-completed"
      <% if(task.get('completed')){ %>
        checked
      <% } %>>
    <%= task.get('title') %>
  </li>
  <% });%>
</ul>